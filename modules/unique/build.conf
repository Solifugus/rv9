# Consumed by tools/build_modules.sh
static_size=304   # sizeof(unique_statics_t): the line and the one before it
revision=1
mtype=program
stack_size=1536     # two lines in statics; nothing deep on the stack

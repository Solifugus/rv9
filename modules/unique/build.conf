# Consumed by tools/build_modules.sh
static_size=400   # sizeof(unique_statics_t): a line of 144 and the one before it
revision=2
mtype=program
stack_size=1536     # two lines in statics; nothing deep on the stack

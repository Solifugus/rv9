static_size=0
revision=4
mtype=program

# True of this program rather than illustrative: it only ever opens
# /gpio/<n>. Declared shared, because a pin may legitimately have several
# readers -- exclusivity is the caller's business, not this command's.
desc="read or set a GPIO"
devices="/gpio"

# Consumed by tools/build_modules.sh
static_size=2560  # sizeof(last_statics_t): a 16-line ring of 144, plus one and the reader
revision=2
mtype=program
stack_size=1536     # the ring is in statics; the stack holds a call or two

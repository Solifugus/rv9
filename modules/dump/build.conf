# Consumed by tools/build_modules.sh
static_size=16    # sizeof(dump_statics_t): one line of bytes
revision=2
mtype=program
stack_size=2048     # measured 1436 used by copy, which has the same shape:
                    # two 48-byte names, an open pair and a chunk loop.
                    # 1536 left it a hundred bytes of headroom and it
                    # overflowed in the foreground.

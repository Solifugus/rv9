static_size=0
stack_size=2048
revision=2
mtype=program

# 15 ms of work declared as 18, so the declaration is a bound and not a
# guess: 180 permille, which with fastloop's 500 stays under the 700
# ceiling. No on_deadline: this loop is the one in the way, not the one
# protected.
desc="a slow loop that does a lot of work each period"
class=realtime
heap_max=0
period_us=100000
deadline_us=100000
wcet_us=18000
mandatory="heap_max"

static_size=0
stack_size=2048
revision=1
mtype=program

# 250 permille declared against 200 used: 20 ms of work declared as 25, so
# the declaration is a bound and not a guess. No on_deadline: this loop is
# the one that is in the way, not the one that is protected.
desc="a slow loop that does a lot of work each period"
class=realtime
heap_max=0
period_us=100000
deadline_us=100000
wcet_us=25000
mandatory="heap_max"

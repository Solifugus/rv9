# /f0 -- persistent storage on a flash partition.
#
# Same file manager as /r0, different driver. Files and loaded programs
# written here survive a reset.
dev_name=/f0
filemgr=rbf
driver=flashdisk
revision=1

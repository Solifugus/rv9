# Consumed by tools/build_modules.sh
static_size=128   # sizeof(count_statics_t)
revision=1
mtype=program
stack_size=1536     # a chunk buffer in statics, nothing deep on the stack; mdir peaks at 1176 of 2048 doing more
                    # work than this. A pipeline is two processes at once and
                    # a session has about 4 KB, so every 512 bytes here is
                    # the difference between composing and not.

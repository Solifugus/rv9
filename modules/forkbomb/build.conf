static_size=64
stack_size=2048
revision=1
mtype=program

# 12 KB for itself and every child. It costs about 2.4 KB and each `deaf`
# about as much again, so it gets four children.
desc="start processes until refused, then clean up"
mem_max=12288
